import pillow as pil# def function_name(first_param, second_param):# 	return result# monochrome# sets the contrast balance of pixelDARK_THRESHOLD = 128def main():	image = SimpleImage('images/girl.jpg')	# go through each pixels one by one:	for pixel in image:		# if pixel is dark enough:		if is_dark_enough(pixel):			# set pixel to be black			pixel.red = 0			pixel.green = 0			pixel.blue = 0		else:			# set pixel to be white			pixel.red = 255			pixel.green = 255			pixel.blue = 255def is_dark_enough(px):	# returns True or False whether pixel is dark enough	# computer avg vlue of pixel	pixel_avg = get_pixel_average(px)	# if pixel_avg < 128:, but we can define constant for 128	if pixel_avg < DARK_THRESHOLD:		return True	else:		return False# takes a pixel and returns avg of given pixeldef get_pixel_average(px):	return (px.red +px)	image.show()if __